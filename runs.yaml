# OPTUNA VERİLERİLERİNDEN DÖNEN VERİLER 
E01:
  seq_len: 1024
  lr_schedule: cosine
  learning_rate: 1.5e-5
  warmup_ratio: 0.035
  weight_decay: 0.05
  rope_scaling: null

  batch_size: 6
  grad_acc: 2
  num_epochs: 2

  eval_split_ratio: 0.01
  max_eval_samples: 2000
  eval_steps: 2000
  save_steps: 2000
  save_total_limit: 2
  logging_steps: 50
  seed: 42

# DAHA HIZLI EĞİTİM İÇİN
E01_fast:
  seq_len: 1024
  lr_schedule: cosine
  learning_rate: 1.5e-5
  warmup_ratio: 0.03
  weight_decay: 0.05
  rope_scaling: null

  batch_size: 6
  grad_acc: 2
  num_epochs: 2

  # 🔥 en büyük hız kazanımı burada
  eval_split_ratio: 0.005        # %1 → %0.5
  max_eval_samples: 1000         # 2000 → 1000
  eval_steps: 6000               # 2000 → 6000
  save_steps: 6000               # 2000 → 6000
  save_total_limit: 2
  logging_steps: 100
  seed: 42
